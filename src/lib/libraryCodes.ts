export const LIBRARY_CODE_TABLE = {
  "146018": "장성공공도서관",
} as { [code: string]: string };

export const LIBRARY_CODES = Object.keys(LIBRARY_CODE_TABLE);
export const LIBRARY_CODE_NAME_PAIRS = Object.entries(LIBRARY_CODE_TABLE);
